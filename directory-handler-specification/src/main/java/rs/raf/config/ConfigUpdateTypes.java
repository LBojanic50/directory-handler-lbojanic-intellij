package rs.raf.config;
/**
 * Enum of config types
 */
public enum ConfigUpdateTypes {
    /**
     * Replace config update type
     */
    REPLACE,
    /**
     * Add config update type
     */
    ADD
}